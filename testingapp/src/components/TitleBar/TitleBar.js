import React, { Component } from 'react';

class TitleBar extends Component {
    render() {
        return <span>
            <h1>This is the TitleBar</h1>
        </span>;
    }
}

export default TitleBar;